(function(){function r(e,n,t){function o(i,f){if(!n[i]){if(!e[i]){var c="function"==typeof require&&require;if(!f&&c)return c(i,!0);if(u)return u(i,!0);var a=new Error("Cannot find module '"+i+"'");throw a.code="MODULE_NOT_FOUND",a}var p=n[i]={exports:{}};e[i][0].call(p.exports,function(r){var n=e[i][1][r];return o(n||r)},p,p.exports,r,e,n,t)}return n[i].exports}for(var u="function"==typeof require&&require,i=0;i<t.length;i++)o(t[i]);return o}return r})()({1:[function(require,module,exports){
'use strict';

var daysElement = document.getElementById('days');
var hoursElement = document.getElementById('hours');
var minutesElement = document.getElementById('minutes');
var secondsElement = document.getElementById('seconds');

var locationElement = document.getElementById('location');

var deadlineSpain = new Date(2019, 3, 26);
var deadlineUSA = new Date(2019, 4, 3);

var timerUpdate = void 0;

var getRemainTime = function getRemainTime(deadline) {

    var now = new Date();

    var totalTime = (new Date(deadline) - now) / 1000;
    var totalDays = Math.floor(totalTime / (3600 * 24));
    var totalHours = Math.ceil(totalTime / 3600 % 24);
    if (totalHours == 24) {
        totalHours = 0;
        totalDays++;
    }

    var totalMinutes = Math.ceil(totalTime / 60 % 60);
    if (totalMinutes == 60) {
        totalMinutes = 0;
    }

    var totalSeconds = Math.ceil(totalTime % 60);

    if (totalSeconds == 60) {
        totalSeconds = 59;
    }

    return {
        totalDays: totalDays,
        totalHours: totalHours,
        totalMinutes: totalMinutes,
        totalSeconds: totalSeconds
    };
};

var setRemainTime = function setRemainTime(location) {
    var timers = location == 'es' ? getRemainTime(deadlineSpain) : getRemainTime(deadlineUSA);

    daysElement.textContent = timers.totalDays;
    hoursElement.textContent = timers.totalHours;
    minutesElement.textContent = timers.totalMinutes;
    secondsElement.textContent = timers.totalSeconds;
};

locationElement.addEventListener('click', function (e) {
    clearInterval(timerUpdate);
    timerUpdate = setInterval(function () {
        setRemainTime(e.target.dataset.country);
    }, 1000);
});

locationElement.childNodes[1].click();
setRemainTime('usa');

},{}]},{},[1]);
